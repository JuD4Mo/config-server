spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: /home/judamo/Projects/microservices-ecommerce/config-data
          clone-on-start: true
          default-label: main
server:
  port: 8888